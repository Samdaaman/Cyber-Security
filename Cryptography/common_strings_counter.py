# Setup
input_file_path = ""
key_length =
# End setup


def run:
    cipher_text_list = []
    for i in range(key_length): # split the cipher text into key_length parts



def split_cipher_text(number):
